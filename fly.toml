# Replace "tg-language-assistant" with the app name generated by `fly launch`.
app = "tg-language-assistant"
primary_region = "cdg"

[build]

[http_service]
  internal_port = 8443
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

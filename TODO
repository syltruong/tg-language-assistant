- [x] html formatting of responses
- [x] keyboard flow
- [x] Reply selector buttons
- [ ] Handle localization of buttons
- [ ] Improve the Correct action, eg. remove the level estiamtion
- [ ] CorrectAction: move to structured_json schema (original/correction/explanation per annotation) so format() can emit rich HTML instead of plain text
- [ ] stale keyboard trigger race: new anchor message + simultaneous keyboard press on old slot → old slot steals active back from new slot; fix by snapshotting active_slot_id before LLM call and skipping deactivation/active-update when session has moved on (branch: feat/edit-in-place-slot)
- [ ] managing when the anchor message is in the base language
- [ ] language pair eval harness (simple, with 20 gold anchor messages)
- [ ] onboarding process for language pairs (consider implementing as a Claude skill)
- [ ] Add telemetry
- [ ] Action.format() should return FormattedResult directly (text + parse_mode together) — removes parse_mode as a separate property and simplifies the runner
- [ ] ResponsePublisher._locks grows one entry per distinct user_id, never evicted — add TTL/LRU eviction once the user base is large enough for this to matter
- [ ] Verify explain-diff-html CI pipeline (explain-diff.yml) actually publishes to gh-pages and comments on PRs
- [ ] Another trivial edit
